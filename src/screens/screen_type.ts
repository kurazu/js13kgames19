const enum ScreenType {
    TITLE,
    CREDITS,
    INTRO,
    RECORDING,
    TIME_LIMIT_EXCEEDED,
    RECORDED,
    WAITING,
    TRAINED,
    COMPETE,
    WON,
    LOST
}

export default ScreenType;
