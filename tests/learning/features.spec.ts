import { getLabel } from '../../src/learning/features';

describe('getLabel', () => {
    test.todo('gets the correct label for up');
    test.todo('gets the correct label for drift');
    test.todo('gets the correct label for left');
    test.todo('gets the correct label for right');
    test.todo('gets the correct label for up and left');
    test.todo('gets the correct lavel for up and right');
});
